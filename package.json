{
  "name": "look-lively",
  "version": "0.0.0",
  "description": "Render a data table, fast & interactive, from Looker",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "webpack-dev-server --open --mode development --no-inline --no-hot --port 8080",
    "stage": "webpack-dev-server --open --mode production --no-inline --no-hot --port 8080",
    "build": "webpack --mode=production"
  },
  "author": "Fabio Beltramini",
  "license": "UNLICENSED",
  "dependencies": {
    "@looker/extension-sdk": "^0.3.4",
    "hyperapp": "^1.2.10"
  },
  "devDependencies": {
    "css-loader": "^6.7.3",
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1",
    "webpack-dev-server": "^3.2.1"
  }
}
